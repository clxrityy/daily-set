REALTIME_ADDR=:8081
# Optional JWT secret; if omitted, anonymous connections are allowed (dev only)
# REALTIME_JWT_SECRET=changeme

# Optional NATS broker URL; if omitted, broker bridging is disabled
# NATS_URL=nats://localhost:4222
